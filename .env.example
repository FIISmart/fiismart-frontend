# Path the front-end calls; relative by default so the Vite dev proxy can reroute it.
VITE_API_URL=/api

# Origin the Vite dev proxy targets. The canonical FIISmart backend listens on :8000.
VITE_API_ORIGIN=http://localhost:8000
